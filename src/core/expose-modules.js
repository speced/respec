// @ts-check
/**
 * @param {string} _name
 * @param {object | Promise<object>} _object
 */
export function expose(_name, _object) {
  // No-op. Previously exposed modules via a legacy AMD require shim
  // for the deprecated w3c-common profile. Kept as a no-op so existing
  // expose() call sites don't need to be removed yet.
}
